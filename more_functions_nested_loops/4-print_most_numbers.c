#include "main.h"

/**
 * print_most_numbers - check believe in number and sing
 * (neither 2 nor 4).
 * Return: no return.
 */
void print_most_numbers(void)
{
  int n=48; 
  for (n = 48 ; n < 58 ; n++);
  if (n != 50 && n != 52)
{
_putchar(n);
}
_putchar('\n');
}
